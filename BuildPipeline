pipeline {
    agent any
    
      environment {
        FOLDER_ID = ''
		GITHUB_USER = ''
        GITHUB_TOKEN = ''
        UE_PATH = 'E:\\UnrealEngine\\UE_5.5'
        WS_PATH = 'E:\\WorldSoul'
    }
    
    stages {
        stage('Pull World Soul') {
            steps {
                ws(env.WS_PATH) {
                powershell '''
                    if (!(Test-Path -Path $env:WS_PATH\\WorldSoul\\))
                    {
                        git clone -b develop https://$env:GITHUB_USER:$env:GITHUB_TOKEN@github.com/WolfPackGameDesign/WorldSoul.git
                    }
                    else
                    {
                        cd $env:WS_PATH\\WorldSoul
                        git fetch origin develop
                        git checkout develop
                        git pull origin develop
                    } 
                    '''
                }
            }
        }
        stage('Generate Project Files World Soul'){
            steps {
                ws(env.WS_PATH)
                {
                    powershell '''
                    & "$env:UE_PATH\\Engine\\Binaries\\DotNET\\UnrealBuildTool\\Unrealbuildtool.exe" `
                    -projectfiles `
                    -project="$env:WS_PATH\\WorldSoul\\WorldSoul.uproject" `
                    -game `
                    -rocket `
                    -progress
                    '''

                }
            }
        }
        stage('Build World Soul'){
            steps {
                ws(env.WS_PATH)
                {
                    powershell '''
                    & "$env:UE_PATH\\Engine\\Build\\BatchFiles\\RunUAT.bat" `
                    -ScriptsForProject="$env:WS_PATH\\WorldSoul\\WorldSoul.uproject" `
                    BuildCookRun `
                    -nop4 `
                    -utf8output `
                    -cook `
                    -project="$env:WS_PATH\\WorldSoul\\WorldSoul.uproject" `
                    -target=WorldSoul `
                    -unrealexe="$env:UE_PATH\\Engine\\Binaries\\Win64\\UnrealEditor-Cmd.exe" `
                    -platform=Win64 `
                    -stage `
                    -archive `
                    -package `
                    -build `
                    -pak `
                    -iostore `
                    -compressed `
                    -prereqs `
                    -archivedirectory="$env:WS_PATH\\WorldSoul\\temp\\Development" `
                    -clientconfig=Development `
                    -nocompileuat `
                    -clean
                    '''
                }
            }
        }
        stage('Zip Build and Upload To Google Drive'){
            steps {
                ws(env.WS_PATH)
                {
                    powershell '''
                    $BuildName = "WorldSoul_Build_$(Get-Date -Format 'yyyyMMdd')_v0.1"   
                    compress-Archive `
                            -Path "$env:WS_PATH\\WorldSoul\\temp\\Development\\*" `
                            -DestinationPath "$env:WS_PATH\\WorldSoul\\temp\\$BuildName.zip `
                            -Force
                    '''
                }
            }
        }
        stage('Upload To Google Drive'){
            steps {
                ws(env.WS_PATH)
                {
                    powershell '''
                        rclone copy "$env:WS_PATH\\WorldSoul\\temp\\Development\\$BuildName.zip" "WS:" --drive-root-folder-id $env:FOLDER_ID               
                    '''
                }
            }
        }                       
    }
}